// fetch('url').then(res => {const newData = mapperService(res); setData(newData)})

// mapperService(res) { return {...res} as то что тебе имеено нужно, чтоб не брать что то лишнееÎ

// : [data, setData] = useState()

// 1. initialState с placeholders
// 2. имитации получения ответа от сервера - передам реальные значения
