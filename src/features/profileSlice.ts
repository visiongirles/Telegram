import { Profile, Settings, Status } from '../interfaces';

const initialProfile: Profile = {
  username: 'placeholder for username',
  profilePicture: 'placeholder for profilePicture', // picture of loading
  status: Status.Online,
};
